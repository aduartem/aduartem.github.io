{
  "name": "aduartem-github-io",
  "version": "1.0.0",
  "description": "Mi sitio web personal",
  "scripts": {
    "minify-js": "gulp minify-js",
    "build": "gulp build",
    "start:dev": "set \"JEKYLL_ENV=development\" && bundle exec jekyll serve",
    "start:prod": "npm run minify-js && set \"JEKYLL_ENV=production\" && bundle exec jekyll serve",
    "build:prod": "npm run minify-js && set \"JEKYLL_ENV=production\" && bundle exec jekyll build --trace",
    "build:prod:linux": "npm run minify-js && JEKYLL_ENV=production bundle exec jekyll build --trace"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2",
    "gulp-concat": "^2.6.1"
  }
}
